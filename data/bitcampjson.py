from responseFile import courtDict

print(courtDict["dockets"])

# store each value of the key in a variable
# find the json files for each value
# store them in variables
# output to a file via linux